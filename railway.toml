# Railway configuration for Polymarket Arbitrage Agent
# TESTING: Using minimal FastAPI app

[build]
# Build command (uses Dockerfile by default)

[deploy]
# Start command - testing with minimal app
start = "uvicorn test_app:app --host 0.0.0.0 --port $PORT"

# Environment variables
[variables]
LOG_LEVEL = "INFO"

# Health check endpoint
HEALTHCHECK_PATH = "/api/health"
